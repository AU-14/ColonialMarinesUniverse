<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:profile="clr-namespace:Content.Client.Lobby.UI.ProfileEditorControls">
    <BoxContainer Name="VBox" Orientation="Vertical">
        <controls:NanoHeading Name="Header" Text="{Loc 'lobby-character-preview-panel-header'}">

        </controls:NanoHeading>
        <BoxContainer Name="Loaded" Orientation="Vertical"
                      Visible="False">
            <Label Name="Summary" HorizontalAlignment="Center" Margin="3 3"/>
            <Label Name="JobSummary" HorizontalAlignment="Center" Margin="3 0 3 3"
                   StyleClasses="CrtHeading" Visible="False"/>
            <BoxContainer Name="ViewBox" Orientation="Horizontal" HorizontalAlignment="Center">
                <profile:ProfilePreviewSpriteView Name="ProfilePreviewSpriteView"
                                                  OverrideDirection="South"
                                                  Scale="4 4"
                                                  MaxSize="112 112"
                                                  Stretch="Fill"
                                                  Access="Public" />
            </BoxContainer>
            <controls:VSpacer/>
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="PrevCharButton" Text="&lt;"
                        ToolTip="{Loc 'lobby-character-preview-prev-char-tooltip'}"
                        Margin="0 5 5 0"/>
                <Button Name="CharacterSetup" Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                        HorizontalAlignment="Center"
                        Margin="0 5 0 0"/>
                <Button Name="NextCharButton" Text="&gt;"
                        ToolTip="{Loc 'lobby-character-preview-next-char-tooltip'}"
                        Margin="5 5 0 0"/>
                <Button Name="PatronPerks" Access="Public" Text="{Loc 'rmc-ui-patron-perks'}"
                        Margin="0 5 0 0" Visible="False"/>
            </BoxContainer>
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="IgnoreAllegianceButton" ToggleMode="True"
                        Text="{Loc 'lobby-character-preview-ignore-allegiance'}"
                        ToolTip="{Loc 'lobby-character-preview-ignore-allegiance-tooltip'}"
                        Margin="0 5 0 0"/>
            </BoxContainer>
        </BoxContainer>
        <Label Name="Unloaded" Text="{Loc 'lobby-character-preview-panel-unloaded-preferences-label'}"/>
    </BoxContainer>
</Control>
