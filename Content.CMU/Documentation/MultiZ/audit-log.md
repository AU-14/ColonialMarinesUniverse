# Multi-Z Audit and Improvement Log

Record every suspected defect, performance issue, or architecture opportunity here during the
functional port. Findings must be recorded before a behavior or architecture change is made.

## Findings

| ID | Subsystem | Finding | Evidence | Recommendation | Status |
| --- | --- | --- | --- | --- | --- |
| MZ-001 | Rendering | Projected lighting is a large per-frame implementation with several source, opening, LOS, candidate, merge, cap, and cleanup passes. | In a 3,600-frame live Bush stair scene, 104 source-to-opening physics rays per frame dominated projected-light candidate work. Replacing the result-materializing ray overload with an exact first-hit broadphase query reduced projected-light p50/p95 from 0.2661/0.3986 ms to 0.2131/0.3307 ms and candidate p50/p95 from 0.2438/0.3579 ms to 0.1896/0.2890 ms. All 374,400 comparator decisions matched, and before/after work remained 104 rays, 9 candidates, and 5 applied lights per frame. A generic occluder substitution was rejected after 12/104 decisions disagreed per frame. | Retain the exact collision-layer/hard-fixture/source-ignore predicate and live comparator evidence. Receiving-map shadow leakage remains MZ-061 and secondary viewports remain MZ-044; do not mix those visual contracts into this performance change. | Improved and measured in Phase 4; visual architecture risks remain |
| MZ-002 | Movement | Shared vertical movement mixes integration-event handling, falling simulation, floor probes, high-ground geometry, vehicle support, transitions, damage, pulling, audio, and debug logging in one module. | `CMUSharedZLevelsSystem.Movement.cs` is 1,930 lines and is called by both movement events and the authoritative server update. The final headless Bush capture recorded `CMU Z Movement` at 0.1778 ms p50 and 0.3270 ms p95 over 300 samples with zero scoped allocation at p95; it did not exercise fall, vehicle, or transition bursts. | Separate only after parity profiling identifies stable seams; retain one authoritative transition interface. | Steady headless sample recorded; burst/latency profiling open |
| MZ-003 | Rendering | Viewport composition contains a large fork-specific projection implementation coupled directly to `ScalingViewport`. | `ScalingViewport.CMUZLevels.cs` is 1,766 lines; `ScalingViewport.Draw`, `ScreenToMap`, and `PixelToMap` have Multi-Z hooks. | Treat viewport composition as one deep module. Compare current content-side viewport facilities before replacing it; do not edit the engine. | Open |
| MZ-004 | Visibility | Visibility and reachability policy is distributed across server PVS probes, viewport passes and snapshots, projected lighting, visible-entity indicators, audio/chat, input, and camera integrations. | Server `CMUZLevelsSystem.View`, `CMUPvsOverrideSystem`, client `ScalingViewport.CMUZLevels`, indicators, projected lighting, speech, audio, and input use related opening geometry but deliberately different evidence and rules. | Share opening geometry and explicit view-plan primitives, then expose separate render, PVS, input, chat, and audio policies. Do not force all consumers into one universal visibility result. | Open |
| MZ-005 | Engine boundary | The old implementation predates the current engine/content rebase. | The port is implemented through content-side partials and adapters; `RobustToolbox/` is intentionally untouched. | Continue preferring current content-side facilities. Treat any necessary engine edit as a separately approved blocker. | Monitoring |
| MZ-006 | Map loading | Automatic Z-network construction must not expose or retain partial topology. | `CMUZNetworkLifecycleSystem` now stages all loads and validation, snapshots caller-owned components, compensates override/topology/auxiliary-`MapInit` exceptions, and rolls back lifecycle-owned maps. Returned-failure and injected-exception integration tests cover the boundary. Public topology notification is explicitly post-commit; see MZ-039. | Retain the lifecycle ownership boundary and fault-injection coverage. | Fixed in Phase 3 |
| MZ-007 | Persistence | A persistence-selected Multi-Z game map loses its extra layer declarations and map overrides. | `GameMapPrototype.Persistence` copies ID, name, replacement path, and stations but not `MapsBelow`, `MapsAbove`, or `ZLevelsComponentOverrides`. `GameMapManager` calls it for a configured persistence map. | Preserve the Multi-Z declarations deliberately or define a persisted-network format; add a prototype clone test before changing persistence behavior. | Open |
| MZ-008 | Networking | Topology is replicated in both directions and repeated again on every map. | `CMUZLevelsNetworkComponent` auto-networks depth-to-map and map-to-depth dictionaries. `CMUZLevelMapComponent` also auto-networks network, depth, map-above, and map-below. Same-tick dirtying can coalesce, so the confirmed cost is duplicated full/late-join state plus runtime topology mutation rather than one packet per `Dirty` call. | Select one canonical replicated topology and derive reverse/neighbor indexes locally only after measuring late-join size and topology-update traffic. | Open |
| MZ-009 | Grid lookup | Core movement, roof, ladder, PVS, weather, and projection paths assume the map entity is the relevant grid and Z levels share world XY/tile indices. | `DistanceToGroundAtWorldPositionCore`, `HasTileAbove`, `HasCurrentTileHighGround`, roof propagation, ladders, and `TryProjectToGround` use `MapGridComponent` on the map UID or carry identical coordinates across maps. The opening cache more generally supports intersecting grids. | Treat aligned single-grid maps as the Bush fast path. Deepen grid resolution behind one world-position query with explicit no-grid semantics before supporting multiple, moving, or rotated grids. | Open |
| MZ-010 | Topology lookup | Fast map/network links fall back to scanning every network, masking invalid, stale, or partially replicated topology state. | `TryGetZNetwork` and `TryMapOffset` enumerate all `CMUZLevelsNetworkComponent` entities after fast paths fail. A successful recovery is returned but does not repair the stale cached map state, so skew can rescan indefinitely. The final eight-viewer capture summed 700 direct network hits, 914 direct neighbour-offset hits, and zero recovery scans, recovered hits, or misses during the 300-tick steady Multi-Z profile. | Capture server/client profiles during load, late join, reconnect, traversal, and rendering. Classify non-Z misses, partial replication, and invariant failures; repair recovered state or disable steady-state scans only when evidence supports it. | Steady Bush fast path profiled; lifecycle/network cases open |
| MZ-011 | Movement logging | Disabled falling diagnostics previously evaluated formatted values in hot movement/probe paths. | A conditional interpolated-string handler now suppresses formatting and formatted-value evaluation, including tile-type conversion, unless diagnostics are active. | Confirm the disabled path in a Phase 4 allocation trace. | Fixed in Phase 3; measurement pending |
| MZ-012 | Movement cost | Ground distance and vehicle-support probes multiply tile/entity work; large footprints still have no overall sample cap. | Vehicle footprint generation now creates each Cartesian sample exactly once, reuses caller lists, removes axis-list allocations and quadratic duplicate scans, and records zero managed allocation in a warmed 1,000-call characterization with sufficient capacity. Each sample still performs support queries. | Profile query counts on real vehicles, then cap or cache only if captures show material cost. | Partially improved in Phase 3 |
| MZ-013 | Transition scheduling | The global transition cap remains order-dependent, but failed attempts previously consumed capacity. | The server now records budget use only after a committed map transfer. Denied transitions retain the parity clamp and client sticky upward transitions have no equivalent server budget. | Capture success, failure, denial, starvation, and correction behavior under burst/latency before changing scheduling. | Partially fixed in Phase 3 |
| MZ-014 | Roof propagation | Initial topology construction and topology removal previously used the same full synchronous roof rebuild; mapping bulk edits can still perform broad work. | Bush initialization necessarily visited and wrote 20,095 tiles in five maps. Bottom-up teardown previously repeated four rebuilds, visited 10 map instances, and wrote 38,298 tiles. A removed-map/depth delta now skips work when no surviving lower level exists and otherwise writes only levels below the removal. The paired teardown recorded zero rebuilds/writes and reduced topology-removal p50 from 0.2816 to 0.0279 ms, while a focused middle-level removal test proved that affected lower roof state is still cleared. | Retain the full initial build and delta-aware removal. Profile a representative middle/upper removal and `znetwork-variantize` bulk edits before batching, caching, capping, or scheduling further work. | Improved and measured in Phase 4; initial/bulk-edit bursts remain |
| MZ-015 | Sprite culling | Dynamic culling performed depth-by-depth spatial queries with incorrect shared opening geometry and persistently owned `Sprite.Visible`. | The unsafe culling system and its CVar were removed in Phase 3. Content has no ownership-aware render filter that can safely distinguish its temporary `false` from another system's later visibility decision. | Build any replacement inside the viewport render pass from its per-depth visibility snapshot, without persisting gameplay sprite state. Profile candidate/opening products before enabling it. | Unsafe mutation removed in Phase 3; render-local replacement deferred |
| MZ-016 | Client visuals | The client scanned every inherited `CMUZPhysics` sprite every update, including stationary/default-state mobs, items, structures, and vehicles. | Active nonzero Z visuals now own a client-only `CMUZPhysicsVisualComponent`; state/startup/move/falling events maintain it, runtime enable performs one repair scan, and the steady-state update enumerates only that active marker. Projectile and follower queries remain limited to their transient components. | Measure active-marker counts and client CPU in Phase 4. Retain the repair pass for runtime enable and late state reconciliation. | Fixed in Phase 3; measurement pending |
| MZ-017 | PVS | Server probe-eye synchronization polls every viewer and owns transient eye entities/subscriber mappings. | The 16/32/64-viewer baseline showed `CMU Z PVS Probes` allocating 106,192/221,136/442,064 bytes per 4-Hz rebuild, while opening sort remained negligible. Server stair-preview and visible-opening LOS now use the allocation-free occluder first-hit API. Post-change allocation was 30,528/62,656/127,552 bytes (71.2-71.7% lower) with identical candidate, LOS, stair-tile, viewer, and probe counts. P50 was 1.2070/2.6280/6.2943 ms before and 0.6432/2.4628/5.0146 ms after. | Capture real packet/bandwidth, late join, reconnect, and populated-round scheduling. The remaining opening materialization and approximately 1.9-2.0 KB per viewer should change only with further evidence; do not add visibility caps or speculative caches. | Improved and measured in Phase 4; real-network/live risks open |
| MZ-018 | Event recipients | Transient projectile and melee effects previously expanded PVS with another session-wide scan. | `AddZLevelViewers` now unions recipients from the maintained probe/subscriber index, including remote origins, without a second attached-session scan. | Measure recipient-union cost and packet fan-out in Phase 4. | Fixed in Phase 3; measurement pending |
| MZ-019 | Opening queries | Opening geometry is cached by chunk, but hot callers repeat bounds assembly, materialize/sort candidates, and use capturing visitors. | One process-wide `CMUZLevelOpeningCache` stores 8x8 masks. `TryFindOpeningBounds` and `FindOpeningCentersNear` repeatedly assemble world bounds; PVS materializes/sorts all candidates before selecting 24. The final eight-viewer snapshot recorded 154 candidates and 78 LOS checks per periodic sample, `VisibleOpening` at 0.0855/0.2218 ms p50/p95 over 304 samples, and `VisibleOpeningSort` at 0.0026/0.0114 ms over 190 samples. See MZ-042. | Retain the process-wide chunk mask and repeat at live scale before specializing delegate, allocation, sorting, or repeated world-bound work. | Headless sample recorded; live-scale profiling open |
| MZ-020 | Atmospherics | No module transfers physical gas, pressure, or heat between maps through Multi-Z openings. | Fire, weather, flame, acid, and similar effects have bespoke ground projection, but each map remains an independent atmosphere simulation. | Confirm isolation as intended parity. If physical cross-Z atmos is desired, specify flow, pressure, heat, fire, and lifecycle behavior before implementation. | Awaiting design |
| MZ-021 | AI/pathfinding | Mobs can physically fall, but no navigation interface models ladders, stairs, falls, or openings as cross-map edges. | Base mobs carry Z physics; route planning remains map-local and traversal is driven by player actions, movement, or bespoke abilities. | Confirm legacy behavior. If AI traversal is required, add explicit cross-map navigation edges rather than recursive map searches. | Awaiting design |
| MZ-022 | Legacy Bush compatibility | Core ship markers are migrated to current prototypes, but removed AU objective/faction semantics are not reproduced. | Active vendor/door/console/lift/destination markers are replaced on map init. Exactly 146 markers remain intentionally unresolved: 144 objective return points plus alliance and withdrawal consoles. | Keep the gap explicit. Port the removed gameplay systems only from an agreed behavior specification; do not approximate them with unrelated current systems. | Known parity gap |
| MZ-023 | Legacy Bush compatibility | Several saved legacy entities use aliases or inert compatibility state rather than exact old behavior. | Compatibility includes current substitutes for legacy consoles/items plus inert `DayNightCycle` and old organ state fields needed for deserialization. | Validate gameplay impact in a live Bush round and retire each alias when a current native prototype or migration is available. | Monitoring |
| MZ-024 | Lifecycle | Removing a map used to leave stale replicated topology and an orphan network. | The focused Bush teardown test reproduced stale network dictionary UIDs. `MapRemovedEvent` handling now removes both indexes, clears neighbors, dirties survivors, refreshes viewers, and deletes an empty network. Integration coverage now removes the middle of a three-level network and verifies the surviving non-contiguous depths, reverse lookup, cleared neighbors, and final teardown. | Retain both full-network and middle-level removal assertions. | Fixed in Phase 1 |
| MZ-025 | Entity storage | Deferred entity-storage work could target entities deleted during map initialization. | Bush loading produced 52 errors until deferred open/close processing verified the entity still had `EntityStorageComponent`. | Retain the defensive component check and cover deferred deletion when an appropriate storage-system test seam is available. | Fixed in Phase 1 |
| MZ-026 | Movement events | Move-event ground snapping could process an entity while it was being inserted into a container and move it between Z-levels before insertion completed. | Transactional topology-before-initialization exposed a `Wrong parent` assertion when `PoweredLightSystem` spawned and inserted bulbs during auxiliary map initialization. The authoritative update already rejected entities whose parent was not their map, but `OnZPhysicsMoveGroundSnapCore` lacked that guard. | Keep the map-parent guard aligned between event-driven ground snapping and the authoritative update. The full Bush initialization test is the regression gate for map-init container insertion. | Fixed in Phase 3 |
| MZ-027 | Lifecycle validation | New-network attachment validation allocated map and depth sets and rechecked invariants already guaranteed by lifecycle-owned inputs. | `CanAttachMapsToZNetwork` received a dictionary, so map keys were already unique; both lifecycle callers generate unique depths; and the target network had just been created empty. Map existence and existing membership are now validated before network creation, and the redundant validator and both set allocations were removed. Final-validation rollback is covered by attempting to build around a base already in another network. | Keep candidate generation and topology mutation inside `CMUZNetworkLifecycleSystem`; reintroduce generic validation only if a genuinely generic attachment API is added. | Fixed in Phase 3 |
| MZ-028 | Shooting/collision | Cross-Z projectile and hitscan adjustment skipped collision from the source muzzle to the selected opening, and the first fix hard-coded `BulletImpassable`. | Source obstruction now uses the selected ammo/hitscan or projectile-prototype collision mask and excludes the shooter. Generic guns defer the check until ammo selection; xeno abilities supply their projectile/line policy before remapping. Focused integration coverage includes ordinary walls, a barricade-only blocker, unobstructed travel, both directions, and visual offsets. | Retain source/destination-map, per-family collision-mask, and visual-offset regression tests. | Fixed in Phase 3 |
| MZ-029 | Shooting/input | A look-up or shoot-down mode could remain active without its adjacent map and cancel every attempted shot. | Public authoritative setters validate the enable CVar and adjacent topology while always permitting disable; impossible modes are refused/cleared, runtime disable clears both modes, and stale impossible state falls back to an ordinary shot. | Retain public-API missing-map and runtime-toggle tests. | Fixed in Phase 3 |
| MZ-030 | Audio | Downward traversal checked the wrong boundary sequence and reused the original source position. | `CMUZLevelAcousticPathBuilder` now models each boundary explicitly and carries the selected opening forward for both directions. Shared unit/integration tests cover multi-step paths. | Retain boundary-direction characterization. | Fixed in Phase 3 |
| MZ-031 | Audio lifecycle | Cross-Z projected audio was created once as unowned static clones. | Supported one-shots now have bidirectional source/projection ownership and rebuild on movement, topology, depth, enable, and audio-CVar changes; shutdown stops projections. Looped sources are explicitly excluded and covered by integration tests. | Define a synchronized loop contract before enabling loop projection. | Fixed for supported one-shots in Phase 3 |
| MZ-032 | Chat rendering | An older same-map guard made cross-Z speech bubbles unreachable. | The obsolete guard was removed and projection now uses the exact eye/map relationship after the Multi-Z eligibility check. | Complete live/UI rendering validation in Phase 4. | Fixed in Phase 3; visual validation pending |
| MZ-033 | Entity indicators | The screen-label pass required a temporary Z eye after viewport composition restored it. | The world pass now publishes per-viewport label positions that the later screen pass consumes without requiring the temporary eye. | Complete multi-viewport visual validation in Phase 4. | Fixed in Phase 3; visual validation pending |
| MZ-034 | Weather rendering | Weather stencil alignment applied one fixed half-tile offset to every eye. | Weather now consumes the active `ZEye.VisualZOffset`; ordinary/base eyes use zero, with focused helper coverage for signed offsets and rotation. | Complete image validation on Bush in Phase 4. | Fixed in Phase 3; visual validation pending |
| MZ-035 | Viewport state | Multi-pass Z rendering mutated eye and clear state without transactional restoration. | `ScalingViewport` saves and restores every mutated field in `try/finally`; fallback paths explicitly render a freshly cleared base pass. Current unit tests cover render-pass eligibility and input-projection policy, but do not execute the graphics-backed restoration or fallback branches. | Add a graphics-capable exception/fallback regression harness and retain live device-loss/error-path validation. | Fixed in code; automated render-state coverage pending |
| MZ-036 | Lighting/render context | Runtime projected-light visibility was controlled by process-global debug telemetry. | Runtime behavior now consumes viewport/eye/map/network-keyed render visibility. `LastZRenderDebugStats` remains telemetry only; projected lighting reads the main viewport snapshot. | Extend projected-light preparation to secondary viewports; see MZ-044. | Fixed for main viewport in Phase 3 |
| MZ-037 | Physics/network/client visuals | `CMUZPhysics` remains a broad replicated capability, but it previously stored both client sprite ownership and server activation-probe cache on every shared instance. | Client ownership capture/restore handles `SnapCardinals`, component/falling shutdown, runtime disable, followers, and predicted-to-replicated projectile handoff compositionally. Presentation ownership now lives on an active-only client component; the tile activation cache is a lazy server-only component. | Measure replicated `CMUZPhysics` counts and late-join bytes before splitting the broad gameplay capability itself. | Process-local caches fixed in Phase 3; network population open |
| MZ-038 | Vehicle prediction | Vehicle horizontal movement is client-predicted, but the transition into falling is discovered only by the server. | Client vehicle movement switches to airborne drift only after replicated `CMUZFallingComponent` arrives. Its shared/client `WakeZPhysics` call is a no-op; authoritative support detection adds the marker on the server. | Test edge traversal under latency. Give prediction deterministic support activation or explicitly suspend horizontal prediction until authoritative falling state arrives. | Needs live validation |
| MZ-039 | Map lifecycle | Network construction lacked exception compensation after validation, while event dispatch cannot be made atomic. | Overrides, topology attachment, and auxiliary initialization now run inside a compensating transaction with caller-owned snapshots and owned-map cleanup. Public topology publication occurs after commit; an injected subscriber exception is logged and the committed network is retained. | Keep the pre-commit/post-commit boundary explicit and subscribers exception-safe. | Fixed boundary in Phase 3 |
| MZ-040 | Topology event flow | A coarse topology event fanned out into network-wide roof work and global power enumeration; removal scanned every network to find one map. | The event now distinguishes a rebuilt network from a removed map/depth. All five Bush teardown removals used the map component's direct network owner with zero recovery scans; removal-scope p50/max/sum fell 90.09%/84.42%/87.40%. During load, the old global power requeue enumerated 47 APCs, 1,174 receivers, and 23 reactor groups in 0.1284 ms even though all 1,221 APC/receiver updates were already pending from MapInit. The topology consumer now queues only the affected network reactor group and preserves the 1,221 legitimate area updates. | Retain the stale-index recovery scan and exact delta consumers. Add a populated reactor-powered-light topology capture and live scheduling evidence before changing the remaining whole light query or power-domain policy. | Improved and measured in Phase 4; reactor-light/live risks open |
| MZ-041 | RMC power | The server reactor-powered-light index accumulated duplicate/stale entries and was never consumed. | The unused index and registration path were removed; the existing authoritative enumeration remains unchanged. | Profile global power recalculation and replace it only with evidence and precise topology deltas. | Fixed in Phase 3 |
| MZ-042 | Opening/visibility architecture | Duplicate process caches were removed, but a safe render-local dynamic-culling consumer remains absent. | All active consumers use one process-wide opening cache. Viewport composition publishes an exact per-view lower-depth snapshot and projected lighting consumes it; the previous independent culling query/mutation path was removed. | If Phase 4 profiles justify dynamic culling, consume the viewport's per-depth snapshot inside the render pass without collapsing render, PVS, input, chat, and audio policy. | Partially improved in Phase 3 |
| MZ-043 | Stair preview/PVS | A counted stair origin at world zero was rejected, and one probe per depth still represents only the first of up to four origins. | Client validity now trusts the replicated count, so `(0,0)` is valid. The multi-origin streaming question remains unmeasured. | Test widely separated stairs at reduced PVS range before choosing one or multiple probes. | Zero-origin fixed in Phase 3; streaming validation pending |
| MZ-044 | Secondary viewports | Multi-Z camera viewports use main-player/global-eye projected-light state. | Surveillance and RMC cameras enable Multi-Z, but projected lighting requires the local player and global eye bounds. Persistent dynamic sprite culling was removed rather than exposing the same incorrect camera-global state. | Prepare projected lighting per viewport, or explicitly disable and document it for secondary viewports. | Confirmed architecture gap |
| MZ-045 | Stair preview performance | Stair preview performs an extra render and unbounded per-entity, per-tile, and LOS work. | The representative live stair scene performed 349 sprite candidate checks and 349 LOS checks per frame. Switching the LOS predicate to the allocation-free occluder first-hit API preserved those counts and reduced stair-cull p50/p95 from 0.4922/0.7479 ms to 0.4277/0.6569 ms; total stair-render p50/p95 fell from 0.8075/1.2290 ms to 0.7382/1.1056 ms. The scene did not exercise nonempty FOV-mask tiles. | Retain work-count instrumentation. Capture a tile-heavy stair scene before considering any cache or budget; no cap or visibility policy change is justified by this result. | Improved and measured in Phase 4; tile-heavy case open |
| MZ-046 | Blur rendering | Every blurred lower-level pass requests the screen texture and performs a full-screen blur. | In the same 1280x720, one-lower-pass stair scene, enabling blur increased frame p50 by 4.26%, render p50 by 5.66%, and lower-pass render p50 by 10.15% relative to blur disabled. The validated after capture recorded one blur pass per frame. No isolated GPU copy/shader timestamp was available, and consolidating sequential multi-depth passes would change accumulated blur strength. | Keep current rendering behavior. Obtain GPU timestamps and an explicit multi-depth visual contract before consolidating, downsampling, or changing blur strength. | Measured in Phase 4; behavior-preserving GPU optimization deferred |
| MZ-047 | Parallax rendering | Parallax suppression keyed off map topology instead of viewport Multi-Z composition. | The parallax adapter now queries the active viewport render context, so ordinary/non-Multi-Z viewports retain parallax even on a Z-network map. | Retain viewport opt-in coverage. | Fixed in Phase 3 |
| MZ-048 | Viewport visibility | Lower-render grace was not keyed to an eye, map, or Z-network. | Grace state now carries the complete view identity and resets on eye/map/network change; focused tests cover identity changes and expiry. | Complete rapid camera-switch visual validation in Phase 4. | Fixed in Phase 3 |
| MZ-049 | Viewer lifecycle | Enabling Multi-Z at runtime omitted players attached while it was disabled. | Enable reconciliation now enumerates attached sessions, creates/refreshes viewer ownership, and disable clears cross-Z state. Connected integration coverage exercises disable/re-enable. | Retain runtime toggle coverage. | Fixed in Phase 3 |
| MZ-050 | Event recipients | Cross-Z effect recipients ignored remote views, repeated a session scan, and could leak ownership if a view moved before removal. | The probe/subscriber index supplies recipients, and `(view, subscriber)` records its add-time viewer so removal works after the view moves off-network. Eye/viewer shutdown uses the same ownership index. Connected integration coverage exercises the move-then-close path. | Measure fan-out and churn in Phase 4. | Fixed in Phase 3 |
| MZ-051 | PVS event flow | A cross-map transform change synchronously rebuilt the same viewer probes twice. | Map/parent changes now mark viewers dirty; the server update coalesces and rebuilds once after transform mutation settles. All 38 final steady snapshots retained eight existing/reused probe eyes, recorded zero created/removed probe eyes or subscriber adds, and needed zero reattachments. | Repeat rebuild/churn counts with cross-map movement, remote views, late join, and reconnect. | Fixed in Phase 3; steady eight-viewer sample clean, lifecycle measurement open |
| MZ-052 | Falling state | A transient server-controller marker is networked and repeatedly added/removed. | `CMUZFallingComponent` is networked; wake/sleep paths churn it. Client visuals also reconcile directly from `CMUZPhysics` state, but falling startup/shutdown and vehicle prediction still use the marker, so replication cannot simply be removed. | Replace it with one explicit prediction-aware active state or a server active set plus the minimum client signal after latency characterization. | Open |
| MZ-053 | Vehicle networking | Twelve immutable vehicle traversal prototype fields were replicated. | Auto-networking was removed from the fields while component presence remains networked; repository-wide assignment review found no runtime mutation contract. | Measure component-state and late-join savings in Phase 4. | Fixed in Phase 3; measurement pending |
| MZ-054 | Mapping persistence | `znetwork-save` is neither atomic nor a reloadable Z-network description. | It writes independent depth YAML files under `ZNetworkSaves`, emits no manifest/prototype with depth declarations, returns after some error cases, and continues after a save failure. A partial set can remain. | Define a manifest-backed, staged/atomic save format and a round-trip test before changing the command. | Open |
| MZ-055 | Acoustic policy | Speech and projected audio duplicated incompatible boundary traversal; delivery and attenuation policies remain intentionally distinct. | A shared acoustic path builder now owns directional boundary/opening traversal. Speech and audio are separate adapters. Audio still repeats legacy 2D attenuation per receiving map and speech retains its view/listener rules. | Specify depth attenuation before changing gameplay and profile delivery allocations in Phase 4. | Boundary model fixed in Phase 3; policy open |
| MZ-056 | Vertical support | Authoritative support and general effect projection still use different surface semantics. | Shared `CanUseZPhysics`/`ShouldWakeZPhysics` now aligns client, server, and thrown-item activation/fallback. Ground distance still includes high ground while `TryProjectToGround` is tile-only. The landing adapter currently has no authoritative thrower to use as `LandEvent` activator. | Characterize high ground across effects and define activator ownership before deepening one support-query module. | Partially improved in Phase 3 |
| MZ-057 | Ordnance cost | Top-down impact resolution allocates and scans the entire Z column on each call. | `TryResolveImpactColumn` creates a result/list and walks every depth/surface. The orbital-cannon update can call it every tick throughout its firing delay; area information can call it repeatedly as well. | Measure whether continuous retargeting is required; cache against target, topology, and tile invalidation if material. | Open |
| MZ-058 | Configuration policy | A rendering-named depth budget controlled server PVS, gameplay audio, and simulation traversal. | Separate `MaxRenderDepth`, `MaxPvsDepth`, and `MaxAudioDepth` CVars now serve their domains; the simulation hard cap is named `MaxZLevelTraversalDepth`. | Tune each budget only from Phase 4 captures. | Fixed in Phase 3 |
| MZ-059 | Architecture/integration | The port has a broad rebase-sensitive integration footprint outside CMU-owned Multi-Z modules. | The current worktree has 46 C# integration hooks outside `Content.CMU/*/ZLevels`: 15 client, 27 shared, and 4 server files, including generic movement, gun, projectile, weather, UI, and camera paths. | Keep small host adapters where control flow requires them; move repeated policy behind CMU-owned systems/events as stable seams emerge. Avoid generic abstractions without a second consumer. | Open |
| MZ-060 | Event flow | `CMUZLevelMoveEvent` was raised on transitions without an in-tree subscriber. | Repository-wide confirmation found no consumer; the event and normal/pulled/carried dispatches were removed. | Reintroduce only with a concrete consumer and contract. | Fixed in Phase 3 |
| MZ-061 | Projected lighting | Projected lights do not cast receiving-map shadows. | Occlusion is tested from source to opening on the source map; cloned/projected lights explicitly disable shadows on the receiving map. Light can therefore visually leak through receiving-level walls. | Treat as a documented legacy approximation until Bush visual tests establish desired behavior; then add receiving-map clipping/shadows if required. | Needs visual validation |
| MZ-062 | Vehicle landing | Vehicle landing repeats interior-occupant discovery and allocates damage specifications per victim. | Landing audio and landing damage independently scan vehicle interior occupants/spatial candidates; damage constructs per-victim specifications. | Maintain canonical occupant membership or share one landing result and union query. Optimize only after vehicle landing profiles. | Open |
| MZ-063 | Prediction/PVS | Predicted cross-Z interaction can request target-map state before the 4-Hz server probe plan has made that level available. | Movement repositions only existing probes, while desired depths are recomputed periodically. Shared cross-Z shooting immediately requires target topology, map, and opening state. | Test immediate fire at openings, view toggles, late join, and reconnect under latency. Add a replicated/PVS-ready plan or prediction gate only if reproduced. | Needs multiplayer validation |
| MZ-064 | Power design | All maps in one Z-network currently share one RMC power domain. | RMC power-group resolution maps every level back to the same Z-network identity, coupling APC/reactor behavior across decks. | Confirm this is legacy gameplay, add a cross-level power-domain test, and preserve or revise it explicitly during power modernization. | Awaiting parity confirmation |
| MZ-065 | Configuration threading | Runtime CVar callbacks can execute away from the simulation thread; direct ECS/cache/view/audio mutation from those callbacks caused integration-test thread assertions. | Enable/depth callbacks now publish reconciliation flags or thread-safe scalars. ECS mutation, cache clearing, probe rebuild, visual restoration, mode clearing, and audio refresh run from `Update`; probe and transition `TimeSpan` values use atomically exchanged ticks. Connected runtime-toggle tests cover the main failure path. | Keep CVar callbacks free of ECS access and add a regression whenever a new runtime Multi-Z CVar is introduced. | Fixed in Phase 3 |
| MZ-066 | Audio filtering | Audio `MoveEvent` fires before `PlayStatic`/`PlayEntity` finishes assigning its recipient filter, so immediate cross-Z projection could turn a private or empty-filter source into a PVS-visible clone. | Move events enqueue the source; the next server update revalidates finalized recipients, global/loop state, source state, and topology before projection. A `Filter.Empty()` integration regression verifies that no cross-Z clone is created. | Retain the deferred filter test and add targeted/private-recipient cases if future audio APIs introduce new filter shapes. | Fixed in Phase 3 |
| MZ-067 | Test architecture | Pure movement, view, and shooting-policy tests used string-based reflection to invoke private methods, bypassing compile-time method and signature checks. | The policy methods are now internal, the shared assembly grants the same test-only friend access already used by the client/server assemblies, and the focused tests call them directly. | Keep deterministic policy seams compile-time checked; prefer integration coverage for ECS orchestration rather than expanding internal policy APIs further. | Fixed in Phase 3 |
| MZ-068 | Runtime sandbox | Normal .NET compilation accepted `ConditionalWeakTable`, `ArrayPool<T>`, and `volatile` metadata that the Robust content sandbox rejects while loading client/shared modules. | The headless module-load repro failed both assemblies before startup. Viewport visibility is now instance-owned with only active viewport IDs tracked during synchronous rendering, roof propagation reuses a system-owned Boolean list, and CVar state uses sandbox-approved `Interlocked` integer access. The same repro now verifies all client-loaded assemblies and proceeds beyond module loading. | Retain a content module-load/type-check gate in validation whenever new runtime/BCL primitives are introduced; ordinary `dotnet build` is not sufficient for sandbox compatibility. | Fixed after Phase 3 validation |
| MZ-069 | Placement rendering | Activating tile or object placement suppressed every Multi-Z viewport pass, so a visible lower deck was replaced by the ordinary space background. | The port retained a pre-fix `IPlacementManager.IsActive` gate in `ScalingViewport.ShouldUseZLevelRenderPasses`, although the legacy branch removed it in `dc4fb34917addb6437802e31a97180ce491b4baf`. The current correction removes placement state from the gate, `ZLevelRenderPassesUseRenderCVarsOnly` asserts that only the enable/render CVars control pass eligibility, and the user confirmed the lower deck remains visible during live placement over the Bush opening. | Retain the render-CVar-only policy regression. | Fixed and live-confirmed in Phase 4 |

## Static performance scan

The Phase 2 static pass covered 67 C# files and 13,865 lines under
`Content.CMU/{Shared,Server,Client}/ZLevels`, plus the directly integrated content systems.

- Six update/frame-update entry points: shared movement/counter flushing, authoritative
  movement/PVS, client visuals, sprite culling, projected lighting, and the compatibility resolver
  while work is queued.
- Sixty-four local-event subscriptions, fifteen local-event raises, twenty ECS query enumerators,
  and eleven spatial-query calls.
- Fourteen `Dirty`, eight `DirtyField`, fourteen `EnsureComp`, and seventeen `QueueDel` call sites.
  These are mutation-shape signals, not packet or CPU measurements.
- Sixteen explicit `new List`/`new Dictionary`/`new HashSet` sites exist in the module. Additional
  hidden allocation paths were found in physics/occlusion query overloads and are recorded in
  MZ-001 and MZ-045.
- No LINQ chains, `ToList`, `ToArray`, regex, culture-sensitive casing, synchronous file I/O, or
  static mutable dictionaries were found in the Multi-Z module.
- Existing counters cover movement, high-ground queries, topology fallback, PVS opening paths,
  render passes, and projected lighting. Runtime behavior no longer consumes process-global debug
  telemetry (MZ-036).

The final Phase 3 .NET scan covered 124 unique production C# files and 30,727 nonblank lines across the
Multi-Z modules, direct modified adapters, and added CMU compatibility systems. It excluded tests,
`RobustToolbox/`, `RSI.NET/`, and generated `BuildChecker/` trees.

- No new Multi-Z string `IndexOf`/`Substring` hot path, repeated replacement chain, or
  current-culture comparer was found.
- Reported `Contains` calls are typed collection/geometry membership. Casing hits belong to
  pre-existing chat/mortar adapter paths rather than new Multi-Z policy.
- Collection creation remains in lifecycle/error paths, command completion, source-owned audio
  registration, and previously documented rendering/ordnance paths. No unrecorded critical
  allocation site was identified.
- The structural scan found 139 explicitly sealed class declarations and no matching plain
  public/internal unsealed declaration in the audited set.

## Phase 4 headless evidence

The full Phase 4 evidence set, `artifacts/multiz-phase4/20260723-230657`, and final 300-tick,
eight-viewer follow-up,
`artifacts/multiz-phase4/20260724-final-profile300-v3/evidence.json`, are interpreted in
[phase-4-validation-log.md](phase-4-validation-log.md). A durable metric/hash summary is checked
in at [evidence/phase-4-baseline-summary.json](evidence/phase-4-baseline-summary.json).

- All hard headless gates passed: the one-map control had no network, Multi-Z Bush loaded all five
  declared depths, and teardown restored map/network counts.
- The full 18,000-tick soak completed. Retained managed bytes decreased by 33,601,008, entities
  stabilized at 15,270 after the first checkpoint, and five maps/one network remained stable.
- The 300-tick steady Multi-Z profile used the direct topology paths without a recovery scan,
  recovered hit, or miss.
- The original full run exposed synthetic-session churn. The final follow-up uses stable observer
  viewers and verifies eight of eight viewers at profile start, throughout all 38 periodic profile
  samples, and at PVS capture in both scenarios, with zero reattachments.
- The process-wide CPU trace was wait/harness dominated and included prototype loading. It did
  not expose a focused CMU Z active-round hot stack.
- No GPU timing, real packet/byte, or two-client measurements were taken; the live client check
  covered placement correctness only.
- The placement render-pass gate was removed and the reported live Bush placement regression was
  user-confirmed fixed.

These results update the applicable finding rows above but do not complete Phase 4.

## Remaining Phase 4 validation backlog

- Repeat the completed 16/32/64-viewer headless PVS scaling capture in a populated live round and
  capture real packet/bandwidth, late-join, and reconnect behavior.
- Run a live two-client server-authority, prediction, reconciliation, late-join, reconnect,
  remote-camera, and latency session.
- Repeat server load/tick captures across order-controlled runs and a populated live Bush round;
  the first whole-scenario single-level/five-level baseline is recorded, but it is not an isolated
  Multi-Z overhead measurement.
- Capture client CPU/frame/allocation and GPU time per Z-level view mode, including projected
  lighting, stair preview, weather, blur, multiple depths, and secondary viewports.
- Capture real network/PVS packets and bytes during level switches, falls, projectile traversal,
  topology changes, and late join.
- Measure replicated component counts and late-join bytes for topology, `CMUZPhysics`, falling
  state, viewer state, and vehicle traversal configuration.
- Capture focused allocations for disabled falling diagnostics, vehicle support, opening/grid
  lookups, PVS recipient filters, stair LOS, projected-light rays, speech, ordnance, and rendering
  hot paths.
- Capture topology lookup counters during initialization, late join, reconnect, traversal,
  topology mutation, and client rendering; the steady headless fast path is already clean.
- Burst-test simultaneous falls, map transitions, roof propagation, mapping edits, and topology
  lifecycle changes.
- Test latency-injected vehicle traversal and immediate cross-Z shooting at an opening.
- Render-pipeline test speech bubbles, indicators, weather offsets, secondary cameras,
  `SnapCardinals`, stair origins, a future render-local culling replacement, blur, and projected
  lighting.
- Live-test audio movement, topology changes, runtime CVar changes, attenuation, and perceived
  timing. Directional multi-depth boundaries, source deletion, and loop exclusion already have
  deterministic integration coverage.
- Explicitly confirm atmosphere isolation, AI non-traversal, and the shared power domain as
  intended gameplay.
